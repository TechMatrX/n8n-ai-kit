# Synology DS218+ phase-1 example
# Copy to .env on the NAS working copy and fill real secrets before first run.

# NAS bind-mount root
NAS_ROOT=/volume1/docker/n8n-ai-kit

# External n8n URL on NAS
WEBHOOK_URL=https://your-nas-n8n.example.com
N8N_HOST=your-nas-n8n.example.com
N8N_PORT=5678
N8N_PROTOCOL=https
N8N_EDITOR_BASE_URL=https://your-nas-n8n.example.com
N8N_SECURE_COOKIE=false
N8N_API_KEY=
N8N_IMAGE=n8nio/n8n:2.23.4
N8N_RUNNERS_IMAGE=n8nio/runners:2.23.4
CLOUDFLARE_TUNNEL_TOKEN=
CLOUDFLARED_IMAGE=cloudflare/cloudflared:latest
MINIO_IMAGE=quay.io/minio/minio:latest
OLLAMA_IMAGE=ollama/ollama:latest
OLLAMA_PULL_MODELS=qwen2.5:1.5b,nomic-embed-text
N8N_RUNNERS_MODE=external
N8N_RUNNERS_BROKER_LISTEN_ADDRESS=0.0.0.0
N8N_NATIVE_PYTHON_RUNNER=true
N8N_RUNNERS_AUTH_TOKEN=replace-with-random-shared-secret
N8N_RUNNERS_LAUNCHER_LOG_LEVEL=info
GENERIC_TIMEZONE=Asia/Ho_Chi_Minh

# n8n secrets
N8N_ENCRYPTION_KEY=replace-with-existing-stable-key
N8N_USER_MANAGEMENT_JWT_SECRET=replace-with-existing-stable-secret
N8N_DEFAULT_BINARY_DATA_MODE=filesystem

# Postgres
POSTGRES_USER=n8n
POSTGRES_PASSWORD=replace-with-strong-password
POSTGRES_DB=n8n

# S3-compatible media artifact storage
MINIO_ROOT_USER=replace-with-minio-root-user
MINIO_ROOT_PASSWORD=replace-with-minio-root-password
MINIO_BROWSER_REDIRECT_URL=
MINIO_SERVER_URL=
MEDIA_ARTIFACT_S3_ENDPOINT=http://100.73.253.62:9000
MEDIA_ARTIFACT_S3_REGION=us-east-1
MEDIA_ARTIFACT_S3_BUCKET=openclaw-media
# Current Phase 1 audio prefix. Future media types should use sibling prefixes:
# generated/image and generated/video.
MEDIA_ARTIFACT_S3_PREFIX=generated/audio
MEDIA_ARTIFACT_S3_ACCESS_KEY_ID=
MEDIA_ARTIFACT_S3_SECRET_ACCESS_KEY=
MEDIA_ARTIFACT_S3_FORCE_PATH_STYLE=true

# Optional runtime values carried from local stack when needed
N8N_RELEASE_DATE=2025-03-06T00:00:00Z
OLLAMA_HOST=http://host.docker.internal:11434

# Phase 1 media worker dispatch
MEDIA_WORKER_BASE_URL=https://media-worker.techmatrx.com
MEDIA_WORKER_INGRESS_TOKEN=
MEDIA_WORKER_CALLBACK_TOKEN=

# Optional Callback v1 -> OpenClaw completion notification
OPENCLAW_MEDIA_COMPLETION_URL=https://openclaw-media.techmatrx.com/internal/media/jobs/complete
OPENCLAW_MEDIA_COMPLETION_TOKEN=

# Contentful-related values only if your workflows need them
CONTENTFUL_MANAGEMENT_TOKEN=
CONTENTFUL_SPACE_ID=
CONTENTFUL_ENV_ID=

# File access restrictions
N8N_RESTRICT_FILE_ACCESS_TO=/home/node/.n8n-files,/backup,/data/shared

# Keep auth/callback defaults explicit
N8N_SKIP_AUTH_ON_OAUTH_CALLBACK=false

# For plain HTTP LAN rehearsal only, temporarily set:
# N8N_SECURE_COOKIE=false
